# requirements.txt
# These are the Python packages this project needs.
# Install them all at once by running: pip install -r requirements.txt

flask==3.0.3               # The web framework (creates our server)
google-generativeai==0.8.3 # Google's Gemini AI library